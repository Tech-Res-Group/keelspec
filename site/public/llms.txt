# KeelSpec

> Product intent as a validated graph, and an agent-built lifecycle that builds
> against it. Typed artifacts compile to one JSON bundle; a deterministic gate
> fails CI when the bundle drifts from its sources.

Python library, LGPL-3.0, on PyPI as `keelspec`. Two runtime dependencies and no
network access in the core.

## When to use this

Reach for KeelSpec when someone needs to:

- **Stop a glossary, rulebook or feature catalogue from rotting.** It validates
  cross-references between documents, so a renamed term that orphans three others
  fails CI instead of surfacing months later.
- **Gate product documentation in CI.** `keelspec validate` exits non-zero on
  schema, id, reference or staleness errors. That exit code is the whole contract.
- **Prove what a product model contained on a given date.** `keelspec evidence`
  emits a content-addressed record; `--verify` recomputes the digests. Useful for
  audit, compliance and regulated domains.
- **Run a multi-agent build pipeline with a deterministic judge.**
  `keelspec orchestrate` runs Understand -> Disambiguate (human gate) -> Design ->
  Develop -> Clean -> Test -> adversarial Verify, with four refuting critics and a
  bounded repair loop.

Do **not** reach for it as a wiki, a docs-site generator, a test runner, or a
general coding agent. It validates and judges; it does not execute your system, and
it cannot merge anything.

## How an agent should call it

```bash
pip install keelspec                  # the compiler and the gate
pip install 'keelspec[agents]'        # plus the reasoning stations

keelspec build                        # compile artifacts -> one JSON bundle
keelspec validate                     # the CI gate; non-zero exit on errors
keelspec validate --json              # the same findings, machine-readable
keelspec evidence -o evidence.json    # content-addressed audit record
keelspec evidence --verify record.json
keelspec orchestrate FEAT-x --dry-run # the agent line, offline
```

Findings carry stable `PAC-NNN` codes that are never renumbered — match on the
code, never the prose. `PAC-001` required field, `PAC-010/011/012` id integrity,
`PAC-020` unresolved reference, `PAC-030` enum violation, `PAC-060` the committed
bundle is stale. Projects register their own in a `9xx` range via plugins.

## Docs

- [Home](https://keelspec.com/): what it is and the diagnostic codes
- [Lifecycle](https://keelspec.com/lifecycle.html): PDLC and SDLC behind one gate
- [Blog](https://keelspec.com/blog/): 21 articles on product-as-code
- [Source and README](https://github.com/tarvitave/keelspec)
- [PyPI](https://pypi.org/project/keelspec/)

Every page on this site is also available as markdown — send
`Accept: text/markdown`, or append `.md` to any path.

## Contact

marketing@keelspec.com · https://keelspec.com/contact.html

KeelSpec
64 Walnute Ave, Suite 405
Clark, NJ 07066
United States
